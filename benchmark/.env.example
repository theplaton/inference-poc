# Copy to benchmark/.env and edit. .env is gitignored so tokens stay local:
#
#   cp benchmark/.env.example benchmark/.env
#
# This layer sits above defaults.env and below the real environment, so
# `MODEL_ID=... ./benchmark.sh` and `./benchmark.sh MODEL_ID=...` both still win
# over whatever is written here. See defaults.env for every other setting and
# its committed value.
#
# Plain KEY=VALUE only -- no quoting, no shell expansion, no inline comments.

# Required. Must match the checkpoint the server was started with -- vLLM
# rejects a request whose model name it is not serving.
MODEL_ID=deepseek-ai/DeepSeek-V4-Pro-0813

# Where the server is. Defaults to localhost:8000; set BASE_URL instead when the
# endpoint needs a scheme or path the host/port pair cannot express.
# HOST=localhost
# PORT=8000
# BASE_URL=http://localhost:8000/v1

# vLLM ignores the key, but a gateway in front of it may not.
# API_KEY=EMPTY
