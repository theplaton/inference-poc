__pycache__/
*.pyc
.venv/
venv/

# Local settings, one per tool folder. Tokens stay out of the repo; the
# committed pair is .env.example (template) and defaults.env (defaults).
.env

# Model weights: repo-local HF hub cache (many GB).
.hf-cache/

# serve_model.sh writes to stdout; this is where you land if you redirect it:
#   ./serve_model.sh > serve.log 2>&1
serve.log
